import Vue from 'vue';
import Vuex from 'vuex';

Vue.use(Vuex);

export default new Vuex.Store({
	state: {},
	modules: {},
	strict: process.env.NODE_ENV === 'development',
	mutations: {},
	actions: {},
});